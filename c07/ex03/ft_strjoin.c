#include <stdio.h>
#include <stdlib.h>

int size_strs(*strs)
{
    int i;

    i = 0;
    while (strs[i])
        i++;
    return (i);
}

char *ft_strjoin(int size, char **strs, char *sep)
{
    int i;
    int wrong
    char *array;

    array = (char *) malloc(sizeof(int) * size_strs(strs));
    if (size == 0)
        return (1);
    i = 0;
    while (i < size)
    {

    }
}

int main(void)
{

}